# Archivo de ejemplo para configuración del entorno
# Copia este archivo como .env y completa con valores reales
# NUNCA subas el archivo .env al repositorio

# Configuración de Base de Datos PostgreSQL (REQUERIDO)
POSTGRES_USER=tu_usuario_db
POSTGRES_PASSWORD=tu_password_seguro
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=tu_nombre_db

# Configuración de la aplicación
DEBUG=True

# Configuración de seguridad (REQUERIDO)
# Genera claves seguras en: https://www.allkeysgenerator.com/Random/Security-Encryption-Key-Generator.aspx
SECRET_KEY=tu_clave_secreta_muy_segura_aqui
JWT_SECRET_KEY=tu_clave_jwt_muy_segura_aqui

# Configuración de autenticación
JWT_ALGORITHM=HS256
JWT_EXPIRATION_TIME=30

# Configuración de PgAdmin (OPCIONAL)
# Si no se configuran, se usarán valores por defecto para desarrollo
PGADMIN_DEFAULT_EMAIL=admin@tuorganizacion.com
PGADMIN_DEFAULT_PASSWORD=tu_password_pgadmin_seguro

# Instrucciones:
# 1. Copia este archivo: cp .env.example .env
# 2. Edita .env con tus valores reales
# 3. Nunca subas .env al git