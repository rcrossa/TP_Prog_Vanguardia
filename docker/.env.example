# Variables de entorno para Docker Compose
# Copia este archivo como .env en el directorio docker/ para usar valores personalizados

# Configuración de PostgreSQL
POSTGRES_DB=reservas
POSTGRES_USER=reservas_user
POSTGRES_PASSWORD=reservas_password

# Configuración de PgAdmin
PGADMIN_DEFAULT_EMAIL=admin@reservas.com
PGADMIN_DEFAULT_PASSWORD=admin123

# Instrucciones:
# 1. Copia este archivo: cp .env.example .env
# 2. Edita .env con tus valores de desarrollo/producción
# 3. Para producción, usa credenciales seguras generadas