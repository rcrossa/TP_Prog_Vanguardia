# =====================================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO - SISTEMA DE RESERVAS
# =====================================================================
# 
# ⚠️ IMPORTANTE: 
# 1. Copiar este archivo como ".env" antes de usar Docker:
#    cp .env.example .env
# 2. NUNCA commitear el archivo .env con credenciales reales
# 3. Cambiar todos los valores por defecto en producción
#
# =====================================================================

# =====================================================================
# CONFIGURACIÓN DE POSTGRESQL
# =====================================================================

# Nombre de la base de datos
POSTGRES_DB=reservas_db

# Usuario de PostgreSQL
POSTGRES_USER=postgres

# ⚠️ CAMBIAR EN PRODUCCIÓN - Password de PostgreSQL
# Generar con: openssl rand -base64 32
POSTGRES_PASSWORD=postgres123

# Host de PostgreSQL (no cambiar si usas Docker)
POSTGRES_HOST=postgres

# Puerto de PostgreSQL
POSTGRES_PORT=5432

# =============================================
# CONFIGURACIÓN DE PGADMIN
# =============================================
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT=5050

# =============================================
# CONFIGURACIÓN DE SERVICIOS
# =============================================

# Python Service (FastAPI)
PYTHON_SERVICE_PORT=8000
PYTHON_SERVICE_HOST=python-service

# Java Service (Spring Boot)
JAVA_SERVICE_PORT=8080
JAVA_SERVICE_HOST=java-service

# =============================================
# LÍMITES DE RECURSOS (DOCKER)
# =============================================

# PostgreSQL
POSTGRES_MEMORY_LIMIT=512m
POSTGRES_MEMORY_RESERVATION=256m
POSTGRES_CPU_LIMIT=0.5

# Python Service
PYTHON_MEMORY_LIMIT=384m
PYTHON_MEMORY_RESERVATION=192m
PYTHON_CPU_LIMIT=0.5

# Java Service
JAVA_MEMORY_LIMIT=512m
JAVA_MEMORY_RESERVATION=256m
JAVA_CPU_LIMIT=0.5
JAVA_XMX=384m
JAVA_XMS=192m

# pgAdmin
PGADMIN_MEMORY_LIMIT=256m
PGADMIN_MEMORY_RESERVATION=128m
PGADMIN_CPU_LIMIT=0.25

# =====================================================================
# CONFIGURACIÓN DE SEGURIDAD
# =====================================================================

# ⚠️ CRÍTICO - Secret key para JWT (CAMBIAR EN PRODUCCIÓN)
# Generar con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# Algoritmo JWT
ALGORITHM=HS256

# Tiempo de expiración del token (minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =====================================================================
# NOTAS DE SEGURIDAD PARA PRODUCCIÓN
# =====================================================================
#
# 🔒 CHECKLIST DE SEGURIDAD:
#
# ✅ 1. Generar SECRET_KEY fuerte:
#       python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# ✅ 2. Usar passwords complejos (mínimo 16 caracteres):
#       - Letras mayúsculas y minúsculas
#       - Números
#       - Símbolos especiales
#
# ✅ 3. Cambiar TODAS las credenciales por defecto:
#       - POSTGRES_PASSWORD
#       - PGADMIN_DEFAULT_PASSWORD
#       - SECRET_KEY
#
# ✅ 4. Desactivar modo debug en producción:
#       DEBUG=false
#
# ✅ 5. Configurar CORS solo para dominios específicos
#
# ✅ 6. Usar HTTPS en producción (certificados SSL/TLS)
#
# ✅ 7. Configurar firewall y rate limiting
#
# ✅ 8. Implementar rotación periódica de secrets
#
# ✅ 9. Habilitar auditoría y logs de acceso
#
# ✅ 10. Realizar backups automáticos y cifrados
#
# ✅ 11. Mantener dependencias actualizadas (npm audit, pip check)
#
# ✅ 12. Revisar permisos de contenedores Docker
#
# =====================================================================

# =============================================
# SEGURIDAD (PYTHON SERVICE)
# =============================================
SECRET_KEY=your-secret-key-change-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================
# INSTRUCCIONES DE USO
# =============================================
# DESARROLLO:
# - Copia este archivo: cp .env.example .env
# - Los valores por defecto funcionan para desarrollo local
#
# PRODUCCIÓN:
# - Cambia TODAS las contraseñas y SECRET_KEY
# - Usa contraseñas seguras (mínimo 16 caracteres)
# - Nunca subas el archivo .env al repositorio
# - Usa secretos del proveedor cloud (AWS Secrets, Azure Key Vault, etc.)